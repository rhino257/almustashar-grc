---
title: "معيار التشفير (Cryptography Standard) — المستشار القانوني الذكي"
doc_id: SEC-CRYPTO-001
version: 0.2
owner: مسؤول الحوكمة / مسؤول أمن المعلومات
status: Draft
approved_by: راعي المشروع
last_review: 2026-07-04
next_review: 2026-12-24
frameworks:
  - { name: "NCA NCS-1", status: محاذاة }
  - { name: "ISO/IEC 27001 A.10", status: محاذاة }
  - { name: PDPL, status: محاذاة }
  - { name: "NIST FIPS 140-3", status: مرجعي }
  - { name: "OWASP ASVS", status: مرجعي }
related:
  - POL-POLICY-001  # سياسة أمن المعلومات
  - ARC-DATA-001    # الحقول المشفّرة (v0.4)
  - ARC-API-001     # المصادقة والرموز (v0.3)
  - GOV-ADR-001     # القرارات التقنية
---

# معيار التشفير

## 1. الغرض والنطاق
يحدّد هذا المعيار الخوارزميات وأطوال المفاتيح وأنماط التشغيل المسموحة
والمحظورة لحماية البيانات أثناء النقل وعند التخزين، وإدارة المفاتيح، في
تطبيق المستشار القانوني الذكي.

## 2. التشفير أثناء النقل (Data in Transit)
- TLS 1.3 (مفضّل)؛ TLS 1.2 حدٌّ أدنى. ما دونه محظور.
- HSTS مفعّل؛ إعادة توجيه HTTP إلى HTTPS.
```text
TLS 1.3 (preferred):
  TLS_AES_256_GCM_SHA384
  TLS_CHACHA20_POLY1305_SHA256
  TLS_AES_128_GCM_SHA256
TLS 1.2 (minimum):
  ECDHE-ECDSA-AES256-GCM-SHA384
  ECDHE-RSA-AES256-GCM-SHA384
Disabled: TLS 1.0/1.1, SSLv3, RC4, 3DES, static-RSA, CBC-only suites
```
> ملاحظة حوكمة: تشفير النقل **لا يُؤجَّل**؛ تكلفته شبه معدومة وهو خط الدفاع
> الأول، خلافاً لتأجيل تشفير التخزين على مستوى الحقل (القسم 9).

## 3. التشفير عند التخزين (Data at Rest)
- الخوارزمية المعتمَدة: **AES-256-GCM** (authenticated encryption).
- التشفير على مستوى الحقل للبيانات الحسّاسة عبر `CryptoService` (القسم 7).
- تشفير القرص/الوحدة (volume encryption) للخادم موصى به كطبقةٍ إضافية.

## 4. التجزئة (Hashing)
| الغرض | الخوارزمية المعتمَدة |
| --- | --- |
| سلامة البيانات / البصمات | SHA-256 أو SHA-384 |
| الأسرار قصيرة العمر (OTP) | **Argon2id** + TTL قصير + تحديد معدّل |
| كلمات المرور | **Argon2id** (مُفعَّل — أو bcrypt كحدٍّ أدنى) |
| رمز التجديد (refresh) | SHA-256 (مخزّنٌ مجزّأً، قابلٌ للإبطال) |
| الفهرس الأعمى / البصمة الحتمية | HMAC-SHA-256 (هاتف/بريد/بريد الاسترداد) |

> تصحيح: لا يُستخدم SHA المجرّد لتخزين OTP؛ يُكسر بالقوة الغاشمة لقصره.
> كلمات المرور مجزّأة بـ Argon2id فعليّاً (غير مؤجّلة)، شأنها شأن TLS.

## 5. التشفير غير المتماثل والتواقيع
| الغرض | المعتمَد |
| --- | --- |
| RSA | مفتاح >= 3072 بت |
| ECDSA/ECDH | منحنى P-256 أو P-384 |
| التواقيع الحديثة | Ed25519 |

## 6. توليد العشوائية
- مولّد عشوائيٍّ آمنٌ تشفيرياً (CSPRNG) حصراً لكل المفاتيح والـ nonces
  وقيم OTP. يُمنع استخدام مولّدات عامة (مثل `random`).

## 7. تطبيق CryptoService (التصميم القابل للتشفير)
- واجهةٌ مجرّدة `Cipher { encrypt(), decrypt() }`.
- التطبيق الحالي: `NullCipher` (تمرير بلا تشفير) — تأجيلٌ مُسجَّل (القسم 9).
- التطبيق المستقبلي: `AesGcmCipher` (AES-256-GCM) دون أي ترحيلٍ للمخطّط
  (الأعمدة `TEXT` مهيّأةٌ لحجم النص المشفّر).
```text
provider switch via env: CIPHER_PROVIDER = null | aes_gcm
key via env/KMS:         CIPHER_KEY = base64(32 bytes)   # required for aes_gcm
```

الحقول المشفّرة على مستوى الحقل (AES-256-GCM عبر CryptoService) مع بصمة
حتمية `HMAC-SHA-256` (`*_hash`) للحقول القابلة للبحث/الفرادة:
| الحقل | الغرض | بصمة HMAC؟ |
| --- | --- | --- |
| `users.phone_enc` | عرض الهاتف (قابل للفكّ الإداري) | نعم (`phone_hash`) |
| `users.email_enc` | عرض البريد الاختياري | نعم (`email_hash`) |
| `users.recovery_email_enc` | عرض بريد الاسترداد | نعم (`recovery_email_hash`) |
| `messages.content` | محتوى الرسائل (سؤال/إجابة) | لا |
| `memories.content` | محتوى الذاكرة الشخصية | لا |

- **تخزين كائنات MinIO** (المرفقات، صور الملفّ): تشفيرٌ عند التخزين على مستوى
  الدلو/الوحدة (bucket/volume at-rest)؛ والوصول عبر روابط موقّعة قصيرة العمر فقط.
- **البصمة/الـ PIN:** وسيلة قفلٍ محلية على الجهاز، **خارج نطاق هذا المعيار**
  (لا تُخزَّن خادمياً ولا تمرّ بالقاعدة) — ARC-API-001 §3.

## 8. إدارة المفاتيح (Key Management)
- توليد المفاتيح بـ CSPRNG وبالأطوال أعلاه.
- التخزين: مدير أسرار/KMS؛ في الـ MVP تُحقن عبر بيئةٍ محميةٍ خارج Git.
- الفصل: مفاتيح مختلفة لكل بيئة (dev/prod)؛ لا مفاتيح في الكود إطلاقاً.
- الدوران (rotation): دوريٌّ وعند الاشتباه بالتسريب؛ دعم إصدارات المفاتيح
  (key versioning) لفكّ النصوص القديمة.

## 9. الامتثال والاستثناءات الحالية
- **استثناء مُسجَّل:** تأجيل تشفير التخزين على مستوى الحقل في الـ MVP
  (`NullCipher`) مع إلزام أن يكون الكود «قابلاً للتشفير» (القسم 7). يُغلق
  الاستثناء عند تفعيل `AesGcmCipher`.
- **غير مشمولٍ بالتأجيل:** تشفير النقل (TLS، القسم 2) وتجزئة كلمات المرور
  بـ Argon2id (القسم 4) والفهرس الأعمى HMAC — كلها إلزاميةٌ فوراً.

## 10. المحظورات
محظورٌ صراحةً: MD5، SHA-1، DES/3DES، RC4، نمط ECB، مفاتيح مضمّنة في
الكود (hardcoded)، وتطوير خوارزميات تشفيرٍ ذاتية (roll-your-own crypto).

## 11. القرارات المفتوحة
| # | البند | الحالة |
| --- | --- | --- |
| 1 | فهرس أعمى (HMAC blind index) للهاتف/البريد/بريد الاسترداد | **مُعتمد** — HMAC-SHA-256 للبحث والفرادة دون نصٍّ صريح |
| 2 | KMS مُدار مقابل حقن البيئة | يُحسم عند الإنتاج |
| 3 | تشفير القرص الكامل للخادم On-Prem | موصى؛ يُؤكَّد مع البنية التحتية |
| 4 | تشفير سرّ TOTP | مؤجّل تبعاً لتأجيل TOTP (ARC-API-001 §17 #5) |

## 12. سجل التغييرات
| الإصدار | التاريخ | الوصف |
| --- | --- | --- |
| 0.1 | 2026-06-24 | المسودّة الأولى لمعيار التشفير (NCS + FIPS + ISO A.10) |
| 0.2 | 2026-07-04 | مواءمة مع ARC-DATA-001 v0.4 / ARC-API-001 v0.3: اعتماد الفهرس الأعمى HMAC للهاتف/البريد/بريد الاسترداد، تفعيل Argon2id لكلمات المرور، توسيع جدول الحقول المشفّرة (البريد، بريد الاسترداد، محتوى الرسائل والذاكرة)، تشفير كائنات MinIO عند التخزين، توضيح استبعاد البصمة/PIN وتأجيل سرّ TOTP |
